/* eslint-disable no-redeclare */
var app = {};

app.init = function() {
	// 检查用户是否已经登录
	//app.login.checkLogin(function(isLogined) {
	//	if (isLogined) {
	app.main.open();
	//	}
	//});
};
app.data = function(data) {
	if (data) {
		return data;
	} else {
		return '暂无数据';
	}
};

